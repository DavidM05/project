const express = require('exprees');
const reouter = express.Router();

const imgmarket = requiere('../../controllers/imgmarket');
module.exports = app => {
    router.get('/market', imgmarket.market); 
}
